version: '3.8'
services:
  db:
    build: ./DbConfig
    ports:
        - 1433:1433

networks:
  default:
    driver: bridge
    internal: false
    attachable: true
    ipam:
      driver: default
      config:
        - subnet: 192.168.0.0/24
          gateway: 192.168.0.1